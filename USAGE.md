# Tdraw 终端绘图工具使用指南

## 功能概述

Tdraw 是一个基于终端的绘图工具，灵感来自于 Excalidraw。它允许用户使用键盘在终端中绘制和操作矩形。

## 安装与运行

### 安装
```bash
cargo build --release
```

### 运行
```bash
cargo run
# 或
./target/release/Tdraw
```

## 基本操作

### 光标移动
- `W` - 向上移动光标
- `A` - 向左移动光标  
- `S` - 向下移动光标
- `D` - 向右移动光标

### 模式切换
- `Ctrl+D` - 进入绘图模式
- `Ctrl+Q` - 退出程序
- `Enter` - 确认/选择
- `Q` - 取消/退出当前模式

## 绘图模式

1. 按下 `Ctrl+D` 进入绘图模式
2. 光标当前位置成为矩形的起始点
3. 使用 `WASD` 调整矩形的宽度和高度
4. 按下 `Enter` 确认绘制
5. 按下 `Q` 取消绘制

## 矩形操作

### 选择矩形
- 将光标移动到矩形内部
- 按下 `Enter` 选择矩形

### 移动矩形
1. 选择矩形后进入移动模式
2. 使用 `WASD` 移动矩形
3. 按下 `Enter` 确认新位置
4. 按下 `Q` 取消移动

### 删除矩形
- 在移动模式下按下 `Backspace` 删除选中的矩形

### 菜单操作
1. 在移动模式下按下 `M` 打开菜单
2. 使用 `W/S` 在菜单项间导航
3. 菜单项包括：
   - 上移一层
   - 下移一层  
   - 水平翻转
   - 垂直翻转
4. 按下 `Enter` 或 `Q` 关闭菜单

## 数据存储

- 绘制的矩形自动保存到 `rects.json` 文件
- 程序启动时自动加载 `rects.json` 中的矩形
- 程序退出时自动保存当前所有矩形

## 文件格式

`rects.json` 文件使用 JSON 格式存储矩形数据：

```json
[
  {
    "x": 10.0,
    "y": 20.0,
    "z": 0.0,
    "width": 30.0,
    "height": 40.0
  }
]
```

- `x`, `y`: 矩形左上角坐标
- `z`: 图层高度（用于确定显示顺序）
- `width`, `height`: 矩形的宽度和高度

## 显示特性

- 白色背景画布
- 黑色圆点光标
- 矩形边框颜色：
  - 黑色：普通矩形
  - 蓝色：光标悬停的矩形
  - 红色：选中的矩形
- 矩形中心显示 z 值（图层高度）
- 屏幕顶部显示当前模式
- 屏幕底部显示操作提示

## 故障排除

### 程序无法启动
- 确保在终端环境中运行
- 检查终端是否支持 ANSI 转义序列
- 确保有足够的终端空间（至少 80x24）

### 键盘输入无响应
- 确保终端处于焦点状态
- 检查键盘布局（使用英文布局）
- 确保没有其他程序捕获键盘输入

### 显示异常
- 调整终端大小后可能需要重启程序
- 确保终端支持颜色显示
- 检查终端字体是否包含所需字符

## 开发说明

### 依赖项
- crossterm: 终端控制
- ratatui: 终端用户界面
- serde/serde_json: 数据序列化

### 项目结构
- `src/main.rs`: 主程序文件
- `rects.json`: 数据存储文件
- `Cargo.toml`: 项目配置

### 扩展功能
程序设计为可扩展的，未来可以添加：
- 更多形状（圆形、线条等）
- 文本输入
- 颜色选择
- 图层管理
- 导入/导出功能