# Tdraw 使用指南

Excalidraw 风格的纯键盘终端矩形画布：白色背景、紫色选中态、悬浮工具栏、上下文菜单和保存状态栏。支持矩形、区块内文字、连接线和有向箭头，暂不包含其他形状或撤回。

## 运行

```bash
cargo run --release
```

在支持 UTF-8 和颜色的交互终端中运行。推荐 80×24 或更大；小窗口会隐藏工具栏，窗口缩放后自动适配。数据文件为**当前工作目录**的 `rects.json`。

## 按键

| 按键 | 操作 |
| --- | --- |
| `w/a/s/d` 或方向键 | 移动光标 / 绘制对角点 / 移动选中区块 |
| `Ctrl+D` | 从光标开始绘制矩形 |
| `Enter` | 选择悬停区块 / 确认绘制或移动 |
| `q` 或 `Esc` | 取消绘制 / 还原本次移动 / 关闭菜单 |
| `Backspace` 或 `Delete` | 删除选中区块并保存 |
| `m` | 在选中状态打开光标旁菜单 |
| `r` | 选中后开始编辑区块文字 |
| `Ctrl+J` | 文字输入模式下换行 |
| `l` | 普通模式下在方框边界开始连线；连线模式下确认终点 |
| `k` | 普通模式下在方框边界开始箭头；箭头模式下确认终点 |
| `Ctrl+S` | 普通模式下重试保存 |
| `Ctrl+Q` | 任意模式退出，取消未确认操作 |
| `Ctrl+Z` | 暂未启用，不改变画布 |

按键使用小写字母。控制键不会被误识别为普通移动键。

## 绘制与选择

1. 光标初始位于终端中央，以黑色圆点显示。
2. 按 `Ctrl+D` 固定起点，用 WASD 调整另一角，四个方向均支持实时预览。
3. 按 `Enter` 保存；按 `q` 放弃。
4. 将光标移入矩形边界或内部，最高图层区块显示蓝色边框。
5. 按 `Enter` 选中，边框变紫；WASD 同时移动区块与光标。
6. 再次 `Enter` 保存位置，图层高度不变；`q` 恢复移动前的位置。

宽高按终端单元格计数，包含两个端点，最小为 1×1。终端字符通常高于宽，因此相同宽高的矩形视觉上不一定是正方形。新建区块置于最高层；同层时后加载的区块优先。区块使用不透明白色填充，超出窗口的部分裁剪显示，不修改数据。光标限制在窗口内。

## 区块文字

1. 将光标移到区块上，按 `Enter` 选中，再按 `r` 开始输入。
2. 已有文字会保留，从末尾继续追加；`Backspace` 删除末尾一个字素，支持组合字符。
3. 文字从方框内部左上角开始显示，按中文等字符的实际终端宽度计算尺寸。超过内部宽度时自动向右扩展，`Ctrl+J` 换行后超过内部高度时自动向下扩展，保留边框空间。
4. 方框只扩展不自动收缩，左上角和图层高度不变；超出窗口时裁剪显示，不自动滚屏。
5. `Enter` 确认文字、尺寸及当前移动位置，立即保存并返回普通模式。
6. `Esc` 恢复输入前的文字和尺寸，返回选中状态；此时再按 `q` 可还原尚未确认的移动。`Ctrl+Q` 放弃所有未确认修改并退出。

**输入模式中 `q/w/a/s/d/r/m/l/k` 都是普通文字**，不触发快捷操作。此时 `Backspace` 仅退格，不删除区块；方向键暂不移动文本插入点。终端原生输入法可用于输入中文。

## 连接线与箭头

1. 先结束移动或文字编辑，回到普通模式，将光标移到方框的**可见边框**上。
2. 按 `l` 开始无向连接线，或按 `k` 开始有向箭头；起点方框变紫。
3. WASD / 方向键自由移动光标，方框保持不动，紫色折线实时预览。
4. 到另一个方框的可见边界，按 `Enter` 或再次按对应的 `l` / `k`，保存连接并回到普通模式。
5. `q` / `Esc` 取消本次连接；`Ctrl+Q` 放弃预览并退出。

只有不同方框之间才能连接；内部、空白处或被上层方框遮住的边界不能作为端点。无效确认不会结束模式，可继续移动。箭头从先选方框指向后选方框，箭头头部位于目标边界外侧。

端点记录“哪条边 + 边上相对位置”，因此移动任一方框或文字撑大方框时自动重新计算。移动/编辑取消也会同步还原连接位置。删除方框会删除所有关联连接，并更新其他连接引用。

连线在方框下方绘制，目前使用简单正交折线，不做障碍避让；路径穿过其他方框或方框重叠时会被遮挡。超出窗口的线段裁剪显示。暂不提供单独选择或删除连接的操作。

### 连接数据格式

`rects.json` 仍为方框数组，起点方框的可选 `connections` 数组保存所有出线：

```json
[
  {
    "x": 5, "y": 5, "z": 0, "width": 10, "height": 5,
    "connections": [
      {
        "target": 1,
        "from": { "side": "right", "offset": 0.5 },
        "to": { "side": "left", "offset": 0.5 },
        "arrow": true
      }
    ]
  },
  { "x": 30, "y": 5, "z": 1, "width": 10, "height": 5 }
]
```

`target` 是当前 JSON 数组中目标方框的下标（从 0 开始）；`side` 为 `top/bottom/left/right`，`offset` 为 0–1。`arrow: false` 表示无箭头连接。手工重排数组时必须同步调整目标下标。旧文件缺少 `connections` 时视为无连接；无效目标或端点会阻止加载，保护原文件。

## 菜单

选中后按 `m`，在光标右侧弹出菜单；空间不足时移至左侧或贴边显示。

- 上移一层
- 下移一层
- 水平翻转
- 垂直翻转

`w/s` 上下循环选择，`a/d` 不移动区块。`Enter` 或 `q` 返回选中状态。以上四项按 README 要求**仅展示，不执行实际操作**。

## 保存与安全

- 启动自动加载 JSON；不存在时使用空画布。
- 确认绘制、确认移动、确认文字、完成连接、删除后立即保存，不必等待退出。
- 先写临时文件，再重命名替换，避免直接截断原文件。
- 保存失败在状态栏显示原因，保留内存数据；普通模式按 `Ctrl+S` 重试。未保存数据写入失败时不会直接退出。
- 无效 JSON 或无效尺寸会阻止启动，且不会覆盖原文件。
- 退出、运行错误或 panic 时恢复终端模式。
- 不支持多个实例同时编辑同一文件。

格式兼容原有的 `x/y/z/width/height` JSON 数组，新增可选 `text` 字段。旧文件缺少 `text` 时按空文字加载，空文字保存时省略该字段，换行保存为 `\n`。首个新建矩形的 `z` 为 0，之后递增。

```json
[
  { "x": 10, "y": 5, "z": 0, "width": 20, "height": 8, "text": "你好 Tdraw\n第二行" }
]
```

## 验证

```bash
cargo test
cargo clippy --all-targets -- -D warnings
```

测试覆盖绘制保存、图层命中、移动回滚、菜单锁定、删除、全局退出、窗口裁剪、无效文件保护，以及中文文字扩展、换行、文字保存加载、取消回滚、字素退格和旧 JSON 兼容；连接测试覆盖边界命中、取消、自动跟随、持久化、箭头渲染、删除清理和索引更新。
