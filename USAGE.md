# Tdraw 使用指南

Excalidraw 风格的纯键盘终端画布：白色背景、紫色选中态、上下文菜单和保存状态栏。支持矩形、圆形/椭圆、菱形、方块内文字、连接线和自适应有向箭头，暂不支持撤回。三种图形统称“方块”（或区块）。顶栏固定显示 `Tdraw | 终端图表绘制工具`，模式及操作提示放在底栏。

## 运行

```bash
cargo run --release
```

在支持 UTF-8 和颜色的交互终端中运行。推荐 80×24 或更大；小窗口会隐藏工具栏，窗口缩放后自动适配。数据文件为**当前工作目录**的 `rects.json`。

## 按键

| 按键 | 操作 |
| --- | --- |
| `w/a/s/d` 或方向键 | 移动光标 / 绘制对角点 / 移动选中区块 |
| `Ctrl+D` | 从光标开始绘制矩形 |
| `Ctrl+W` | 从光标开始绘制圆形/椭圆 |
| `Ctrl+A` | 从光标开始绘制菱形（四角朝上下左右） |
| `Enter` | 选择悬停区块或连接 / 确认绘制或移动 / 取消连接选择 |
| `q` 或 `Esc` | 取消绘制 / 还原本次移动 / 关闭菜单 / 取消连接选择 |
| `Backspace` 或 `Delete` | 删除选中区块或连接并保存（文字模式下 Backspace 仅退格） |
| `m` | 在选中状态打开光标旁菜单 |
| `r` | 选中后开始编辑区块文字 |
| `Ctrl+J` | 文字输入模式下换行 |
| `l` | 在方框边界开始连线；连线模式下确认终点或添加中间点 |
| `k` | 在方框边界开始箭头；箭头模式下确认终点或添加中间点 |
| `Ctrl+S` | 普通模式下重试保存 |
| `Ctrl+Q` | 任意模式退出，取消未确认操作 |
| `Ctrl+Z` | 暂未启用，不改变画布 |

按键使用小写字母。控制键不会被误识别为普通移动键。

## 绘制与选择

1. 光标初始位于终端中央，以黑色圆点显示。
2. 按 `Ctrl+D` / `Ctrl+W` / `Ctrl+A` 选择矩形 / 圆形 / 菱形并固定外接框起点，用 WASD 调整另一角，自由改变宽高，四个方向均支持实时预览。
3. 按 `Enter` 保存；按 `q` 放弃。
4. 将光标移入方块实际边界或内部，最高图层方块显示蓝色轮廓。圆形和菱形外接框的空白角落保持透明，不会挡住下方图形或连线的选择。
5. 按 `Enter` 选中，边框变紫；WASD 同时移动区块与光标。
6. 再次 `Enter` 保存位置，图层高度不变；`q` 恢复移动前的位置。

宽高是外接框的终端单元格数量，包含两个端点，最小为 1×1（极小时轮廓会退化）。终端字符通常高于宽，因此圆形允许独立调整宽高，形成椭圆；视觉圆形通常需要约 2:1 的列数/行数。菱形四个顶点始终位于外接框的上、右、下、左方向。新建区块置于最高层；同层时后加载的区块优先。区块使用不透明白色填充，超出窗口的部分裁剪显示，不修改数据。光标限制在窗口内。

## 区块文字

1. 将光标移到区块上，按 `Enter` 选中，再按 `r` 开始输入。
2. 已有文字会保留，从末尾继续追加；`Backspace` 删除末尾一个字素，支持组合字符。
3. 矩形文字从内部左上角显示；圆形/菱形使用中央约三分之一宽高的安全矩形区域，并额外留出轮廓与输入光标的空间。按中文等字符的实际终端宽度计算需求，超过安全区域宽度时自动向右扩展外接框，`Ctrl+J` 换行后自动扩展高度。保留图形类型，文字不会挤到弧线或斜边上。此布局偏重可读性，因此圆形/菱形输入长文本时尺寸会比矩形更大。
4. 方框只扩展不自动收缩，左上角和图层高度不变；超出窗口时裁剪显示，不自动滚屏。
5. `Enter` 确认文字、尺寸及当前移动位置，立即保存并返回普通模式。
6. `Esc` 恢复输入前的文字和尺寸，返回选中状态；此时再按 `q` 可还原尚未确认的移动。`Ctrl+Q` 放弃所有未确认修改并退出。

**输入模式中 `q/w/a/s/d/r/m/l/k` 都是普通文字**，不触发快捷操作。此时 `Backspace` 仅退格，不删除区块；方向键暂不移动文本插入点。终端原生输入法可用于输入中文。

## 连接线与箭头

1. 先结束移动或文字编辑，回到普通模式，将光标移到方框的**可见边框**上。
2. 按 `l` 开始无向连接线，或按 `k` 开始有向箭头；起点方框变紫。
3. WASD / 方向键自由移动光标，方框保持不动，紫色折线实时预览。
4. 在非目标边界位置再次按对应的 `l` / `k`，记录一个中间点并继续连线。可以连续添加多个点，预览以 `◆` 标记已记录点；连续在同一位置重复按键不重复添加。
5. 到另一个方框的可见边界，按 `Enter` 或再次按对应的 `l` / `k`，保存整条连接并回到普通模式；这时优先连接方框，不添加中间点。
6. `q` / `Esc` 取消本次连接及全部中间点；`Ctrl+Q` 放弃预览并退出。未完成的中间点不会写入文件。

只有不同方框之间才能连接；内部、空白处或被上层方框遮住的边界不能作为端点。`Enter` 在无效终点处不会结束模式，也不添加中间点；只有当前工具对应的 `l` 或 `k` 才会记录中间点。箭头从先选方框指向后选方框，箭头头部位于目标边界外侧。

路径按添加顺序经过所有中间点，相邻点不在同一行/列时自动补齐正交折段。不添加中间点时自动生成正交折线路径。中间点固定在画布绝对位置，移动方框或文字撑大方框时不跟随移动，只有连接端点重新计算。方框内部也可记录中间点，但该部分路径会被方框遮挡；推荐把中间点放在空白处以绕开方框。

**普通连线**保留用户所选的“哪条边 + 边上相对位置”，随方块移动或扩展重新计算轮廓上的端点。

**有向箭头**自动选择更合适的附着边：没有中间点时两端朝向对方方块；有中间点时起点朝向第一个中间点，终点朝向最后一个中间点。移动源方块或目标方块、文字扩展尺寸时，箭头会切换到目标上/下/左/右侧，头部位于实际轮廓外并指向目标，而不是一直固定在旧边。箭头的实际连接位置可能与最初点击的位置不同。矩形、圆形和菱形都支持此行为，旧存档中的箭头同样生效。

移动/编辑取消也会同步还原连接位置和方向。删除方块会删除所有关联连接，并更新其他连接引用。

连线在方框下方绘制，使用圆角正交折线：根据线段实际相接方向显示 `╭╮╰╯`，共线段保持直线，仅真正交叉或分支处显示 `┼/├/┤/┬/┴`，重复中间点不再产生伪十字。预览与保存后的连接使用同一套绘制逻辑，预览中已固定的中间点仍以 `◆` 标记。当前不做障碍避让；路径穿过其他方框或方框重叠时会被遮挡。超出窗口的线段裁剪显示。

### 选择与删除连接

1. 在普通模式下，把光标移到方框外的可见线段、折点或箭头头部上，命中的连接变蓝。
2. 按 `Enter` 选中，整条连接变紫，状态栏显示“连接已选中”。
3. 按 `Backspace` 或 `Delete` 删除当前连接，立即保存并返回普通模式；两端方框和其他连接保持不变。
4. `q` / `Esc` / `Enter` 取消选择，不修改画布；`Ctrl+Q` 退出也不会删除连接。

选中后 WASD 只移动光标，选择保持不变；连接端点仍绑定原方框，不支持单独拖动。方框边框或内部优先选择方框，被方框遮挡的线段不可选。多条连接交叉或重叠时，选中最后绘制的那条（按方框数组及各自连接数组顺序）；可移到不重叠的部分选择其他连接。删除不支持撤回。

### 连接数据格式

`rects.json` 仍为方框数组，起点方框的可选 `connections` 数组保存所有出线：

```json
[
  {
    "x": 5, "y": 5, "z": 0, "width": 10, "height": 5,
    "connections": [
      {
        "target": 1,
        "from": { "side": "right", "offset": 0.5 },
        "to": { "side": "left", "offset": 0.5 },
        "arrow": true,
        "waypoints": [[20, 12], [25, 12]]
      }
    ]
  },
  { "x": 30, "y": 5, "z": 1, "width": 10, "height": 5 }
]
```

`target` 是当前 JSON 数组中目标方框的下标（从 0 开始）；`side` 为 `top/bottom/left/right`，`offset` 为 0–1。`arrow: false` 表示无箭头连接。手工重排数组时必须同步调整目标下标。`waypoints` 为可选中间点数组，每项是 `[x, y]`，要求非负有限坐标；缺少或为空时使用原来的自动路径。旧文件缺少 `connections` 时视为无连接；无效目标或端点会阻止加载，保护原文件。

## 菜单

选中后按 `m`，在光标右侧弹出菜单；空间不足时移至左侧或贴边显示。

- 上移一层
- 下移一层
- 水平翻转
- 垂直翻转

`w/s` 上下循环选择，`a/d` 不移动区块。`Enter` 或 `q` 返回选中状态。以上四项按 README 要求**仅展示，不执行实际操作**。

## 保存与安全

- 启动自动加载 JSON；不存在时使用空画布。
- 确认绘制、确认移动、确认文字、完成连接、删除后立即保存，不必等待退出。
- 先写临时文件，再重命名替换，避免直接截断原文件。
- 保存失败在状态栏显示原因，保留内存数据；普通模式按 `Ctrl+S` 重试。未保存数据写入失败时不会直接退出。
- 无效 JSON 或无效尺寸会阻止启动，且不会覆盖原文件。
- 退出、运行错误或 panic 时恢复终端模式。
- 不支持多个实例同时编辑同一文件。

格式兼容原有的 `x/y/z/width/height` JSON 数组。新增 `shape` 字段支持 `rectangle`、`ellipse`、`diamond`；旧文件缺少 `shape` 时按矩形加载。`width/height` 始终表示外接框尺寸。支持可选 `text` 字段。旧文件缺少 `text` 时按空文字加载，空文字保存时省略该字段，换行保存为 `\n`。首个新建矩形的 `z` 为 0，之后递增。

```json
[
  { "x": 10, "y": 5, "z": 0, "width": 20, "height": 8, "text": "你好 Tdraw\n第二行" }
]
```

## 代码结构

```text
src/
├── main.rs              # 程序入口、终端恢复及事件循环
├── model.rs             # 统一 Shape 模型、三种轮廓、文字安全区域
├── storage.rs           # JSON 加载校验、原子替换保存
├── app/
│   ├── mod.rs           # 应用状态、选择命中、取消与提交
│   ├── input.rs         # 各模式键盘交互
│   └── tests/           # 核心操作、形状、连接和中间点回归测试
├── connection.rs        # 连接模型、箭头自适应端点、路径与命中计算
├── connection/
│   ├── anchor.rs        # 实际轮廓锚点、朝向计算
│   └── render.rs        # 线段方向合并、圆角/交叉绘制及测试
└── ui/
    ├── mod.rs           # 画布、标题栏、菜单与预览
    ├── shapes.rs        # 三种方块轮廓及文字绘制
    └── theme.rs         # 配色和菜单文案
```

路径计算和选择命中共享几何数据；渲染只扫描可见行列，不遍历屏幕外的长线。原存档可直接使用；新存档增加形状类型字段，旧文件未声明形状时仍为矩形。

## 验证

```bash
cargo test
cargo clippy --all-targets -- -D warnings
```

测试覆盖绘制保存、图层命中、移动回滚、菜单锁定、删除、全局退出、窗口裁剪、无效文件保护，以及中文文字扩展、换行、文字保存加载、取消回滚、字素退格和旧 JSON 兼容；连接测试覆盖边界命中、取消、自动跟随、持久化、箭头渲染、删除清理和索引更新，以及连接悬停/选中颜色、两种删除键、取消选择、折线/箭头头部命中、方框遮挡、重叠优先级和删除后的持久化；中间点测试覆盖多个转折点、重复点过滤、预览、保存加载、取消、移动端点后保留中间点、折线路径选择删除以及旧格式兼容。圆角渲染测试覆盖四种转角、重复点、共线重叠、真实交叉、屏幕裁剪及完整连接的圆角输出。形状测试覆盖圆形/菱形的绘制、中文和多行文字安全边距、不同宽高的轮廓锚点、保存加载、删除，以及箭头随相对位置变化自动切换四个方向。
